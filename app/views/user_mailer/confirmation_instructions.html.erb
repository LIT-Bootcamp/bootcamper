<p><%= t("devise.mailer.confirmation_instructions.greeting", email: @resource.email) %></p>

<p><%= t("devise.mailer.confirmation_instructions.intro") %></p>

<p><%= link_to t("devise.mailer.confirmation_instructions.action"), Rails.application.routes.url_helpers.confirmation_url(confirmation_token: @token, **Rails.application.config.action_mailer.default_url_options) %></p>
